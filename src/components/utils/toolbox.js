export async function httpFetchRequest(query, variables = {}){
    console.log('function imported')
}

export class MenuDislay{
    constructor(input, target){
        this.input = input,
        this.target = target
    }
}